import { createTheme } from "@material-ui/core";
import '../fonts.css';


const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(18, 19, 30)'
        },
        secondary: {
            main: '#E879F9',
            light: '#8892B0'
        }
    },

})

export default theme;